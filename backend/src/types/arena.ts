export interface ArenaStats {
  arenaId: string;
  currentPot: number;
  playerCount: number;
  survivorCount: number;
  currentRound: number;
  entryFee: number;
  yieldAccrued: number;
  status: string;
  lastUpdated: string;
}
